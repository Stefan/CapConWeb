/** @deprecated Use `@/i18n/get-dictionary` and `useSite()` instead. */
export {
  appLoginUrl,
  appSignupUrl,
  contactEmail,
  demoRequestUrl,
  PRODUCT_NAME,
  PRODUCT_TAGLINE,
} from "@/lib/brand";
